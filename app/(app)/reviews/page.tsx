import { EmptyState } from "@/src/components/ui/empty-state";

export default function ReviewsPage() {
  return (
    <EmptyState
      title="Weekly & monthly reviews"
      description="Step back, spot patterns, and turn hindsight into process. Structured review templates are on the way."
    />
  );
}
