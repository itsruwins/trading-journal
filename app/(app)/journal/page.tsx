import { EmptyState } from "@/src/components/ui/empty-state";

export default function JournalPage() {
  return (
    <EmptyState
      title="Daily journal"
      description="Capture pre-market plans, in-session notes, and end-of-day reflections — kept alongside the trades they belong to."
    />
  );
}
