import { EmptyState } from "@/src/components/ui/empty-state";

export default function SettingsPage() {
  return (
    <EmptyState
      title="Settings"
      description="Your profile, trading accounts, and tags will be managed here — including avatar upload and account preferences."
    />
  );
}
