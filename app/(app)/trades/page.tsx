import { EmptyState } from "@/src/components/ui/empty-state";

export default function TradesPage() {
  return (
    <EmptyState
      title="Your trade log lives here"
      description="Every entry, exit, screenshot, and tag in one place. Trade logging arrives in the next phase — the table, filters, and detail views are ready for it."
    />
  );
}
