import { EmptyState } from "@/src/components/ui/empty-state";

export default function AnalyticsPage() {
  return (
    <EmptyState
      title="Analytics on your edge"
      description="Win rate, profit factor, expectancy, and performance broken down by setup and tag — all computed from your trade history once it exists."
    />
  );
}
